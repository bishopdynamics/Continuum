# copyrighted Half-Life assets — never distribute (see research/00)
gamedata/
install/

# research/ is the old docs/ tree: research notes, mockups, design docs.
# Kept locally, not shipped. User-facing docs live in doc/ (committed).
research/

# derived locally from the user's assets — never ship (derived-work risk)
cache/

# xash3d-fwgs/ and hlsdk-portable/ are now git submodules (see .gitmodules)

# hlsdk expansion build worktrees (recreated from the hlsdk-portable submodule)
hlsdk-branches/

# local build deps (SDL2 from source)
.deps/

.claude/

# build_all.sh output (staging + artifacts)
/dist/

Elefant-Offline.md

# assets for some games, for us to study and understand. must not commit
game-reference/

# for testing dist, must not commit
dist-test/

# source GIMP files for image assets
assets-src/

tmp/

# macOS Finder metadata
.DS_Store
