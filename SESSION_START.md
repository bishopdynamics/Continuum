IMPORTANT: investigate all available skills, particularly Elefant and dev-tools. 

Elefant is your auxillary memory system, and dev-tools provide helpful tools for development tasks. 
You must familiarize yourself with all available skills and tools at the beginning of every session.

Use this project's folder name as a keyword or tag when searching for information. You won't find much in wiki, but there is a lot of stuff in memories.

This project was previously named "xash-streaming" and so many of the memories in Elefant will be tagged with that project name, instead of the current name "xash3-continuum"

You _must_ consult Elefant for relevant information prior to taking any actions
You _must_ create memories in Elefant as you go, so that no progress information is lost between sessions

NOTE: you must not use `rm -f` the `-f` flag is not allowed, and if you try a command that contains that usage, the whole command will be denied. Just do it without `-f` and it will work.

The file "Elefant-Offline.md" is our local cache for writing Elefant memories when Elefant is offline; read it. 
If you find memories there, try to add them to Elefant, and if successful: remove them from the local cache.

Read Notes.md for my current notes
